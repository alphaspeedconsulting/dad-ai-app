# Publishes the Next.js static export (dad-alpha/out) to GitHub Pages.
# Repo: dad-ai-app — same pattern as mom-ai-app.
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: dad-alpha/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: dad-alpha

      - name: Build static export
        run: npm run build
        working-directory: dad-alpha
        env:
          NEXT_PUBLIC_API_URL: https://household-alpha-api.onrender.com

      - name: Disable Jekyll (allow _next and dotfiles)
        run: touch out/.nojekyll
        working-directory: dad-alpha

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dad-alpha/out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
